---
layout:     post
title:      leetcode每日一题——3508.设计路由器
date:       2025-09-20
subtitle:   设计路由器，主要是设计class以及二分查找+STL各种容器的运用，这里做个回顾
author:     蓝
categories: 算法
header-img: img/algorithms/暴力赞妮.jpg
catalog: true
tags: 
  - 刷题
  - 算法
---


# 1. 问题背景与需求分析
<style>
details[open] summary .if-collapsed { display: none; }
details:not([open]) summary .if-expanded { display: none; }
</style>
<details markdown="1">
<summary><span class="if-collapsed">点击展开题目描述</span><span class="if-expanded">点击收起题目描述</span></summary>

## 请你设计一个数据结构来高效管理网络路由器中的数据包。每个数据包包含以下属性：

- **source**：生成该数据包的机器的唯一标识符。
- **destination**：目标机器的唯一标识符。
- **timestamp**：该数据包到达路由器的时间戳。

### 实现 Router 类：

- `Router(int memoryLimit)`：初始化路由器对象，并设置固定的内存限制。memoryLimit 是路由器在任意时间点可以存储的最大数据包数量。如果添加一个新数据包会超过这个限制，则必须移除最旧的数据包以腾出空间。
- `bool addPacket(int source, int destination, int timestamp)`：将具有给定属性的数据包添加到路由器。如果路由器中已经存在一个具有相同 source、destination 和 timestamp 的数据包，则视为重复数据包。如果数据包成功添加（即不是重复数据包），返回 true；否则返回 false。
- `int[] forwardPacket()`：以 FIFO（先进先出）顺序转发下一个数据包。从存储中移除该数据包。以数组 [source, destination, timestamp] 的形式返回该数据包。如果没有数据包可以转发，则返回空数组。
- `int getCount(int destination, int startTime, int endTime)`：返回当前存储在路由器中（即尚未转发）的，且目标地址为指定 destination 且时间戳在范围 [startTime, endTime]（包括两端）内的数据包数量。

**注意**：对于 addPacket 的查询会按照 timestamp 的递增顺序进行。

<details markdown="1">
<summary>示例 1</summary>

输入：

```
["Router", "addPacket", "addPacket", "addPacket", "addPacket", "addPacket", "forwardPacket", "addPacket", "getCount"]
[[3], [1, 4, 90], [2, 5, 90], [1, 4, 90], [3, 5, 95], [4, 5, 105], [], [5, 2, 110], [5, 100, 110]]
```

输出：

```
[null, true, true, false, true, true, [2, 5, 90], true, 1]
```

解释：

```cpp
Router router = new Router(3); // 初始化路由器，内存限制为 3。
router.addPacket(1, 4, 90); // 数据包被添加，返回 True。
router.addPacket(2, 5, 90); // 数据包被添加，返回 True。
router.addPacket(1, 4, 90); // 这是一个重复数据包，返回 False。
router.addPacket(3, 5, 95); // 数据包被添加，返回 True。
router.addPacket(4, 5, 105); // 数据包被添加，[1, 4, 90] 被移除，因为数据包数量超过限制，返回 True。
router.forwardPacket(); // 转发数据包 [2, 5, 90] 并将其从路由器中移除。
router.addPacket(5, 2, 110); // 数据包被添加，返回 True。
router.getCount(5, 100, 110); // 唯一目标地址为 5 且时间在 [100, 110] 范围内的数据包是 [4, 5, 105]，返回 1。
```
</details>

<details>
<summary>示例 2</summary>

输入：

```
["Router", "addPacket", "forwardPacket", "forwardPacket"]
[[2], [7, 4, 90], [], []]
```

输出：

```
[null, true, [7, 4, 90], []]
```

解释：

```cpp
Router router = new Router(2); // 初始化路由器，内存限制为 2。
router.addPacket(7, 4, 90); // 返回 True。
router.forwardPacket(); // 返回 [7, 4, 90]。
router.forwardPacket(); // 没有数据包可以转发，返回 []。
```
</details>

**提示：**

- 2 <= memoryLimit <= 10^5
- 1 <= source, destination <= 2 * 10^5
- 1 <= timestamp <= 10^9
- 1 <= startTime <= endTime <= 10^9
- addPacket、forwardPacket 和 getCount 方法的总调用次数最多为 10^5。
- 对于 addPacket 的查询，timestamp 按递增顺序给出。

</details>

# 2. 核心数据结构设计

## 2.1 使用 tuple 表示数据包（方法一）—— 参考大神[灵茶山艾府](https://leetcode.cn/u/endlesscheng/)

```cpp
#include <tuple>
using Packet = std::tuple<int, int, int>; // source, destination, timestamp
```

tuple 是一个可以存储多个不同类型元素的容器，类似于 pair 但更加灵活，能够将三个整型数据组合成一个单一对象。

### 2.1.1 整体架构

``` cpp
class Router {
    int memory_limit;
    queue<tuple<int, int, int>> packet_q; // packet 队列
    // 注：如果不想手写 TupleHash，可以用 set
    unordered_set<tuple<int, int, int>, TupleHash> packet_set; // packet 集合
    // deque 支持随机访问，方便二分
    unordered_map<int, deque<int>> dest_to_timestamps; // destination -> [timestamp]

public:
    Router(int memoryLimit) {
    }

    bool addPacket(int source, int destination, int timestamp) {
    }

    vector<int> forwardPacket() {
    }

    int getCount(int destination, int startTime, int endTime) {
    }
};
```

## 2.2 采用结构体 Packet

其实思路基本一样，但最后自己重载符号

```cpp
class Router {
private:
    int memory_limit;
    std::queue<Packet> packet_queue; // FIFO队列
    std::unordered_set<Packet, PacketHash> packet_set; // 快速查重
    std::unordered_map<int, std::deque<int>> dest_to_timestamps; // 目标地址到时间戳映射
public:
    // 类方法实现
    Router(int memoryLimit) {
    }
    
    bool addPacket(int source, int destination, int timestamp) {
    }
    
    vector<int> forwardPacket() {
    }
    
    int getCount(int destination, int startTime, int endTime) {   
    }
};
```

# 3. 运算符重载的应用

## 3.1 运算符重载知识点
> **PS：** 博主这里想回顾一下过去知识，若已知晓可跳过。
<details>
<font style="font-weight:bold;">重载运算符（Operator Overloading）</font>是 C++ 允许自定义类型（如结构体、类）定义自己的运算符行为的机制。常见的重载运算符有比较运算符（如 `<`, `==`）、算术运算符（如 `+`, `-`）等。

### 1. 基本语法

运算符重载通常以成员函数或友元函数的形式实现。例如：

```cpp
struct Point {
  int x, y;
  bool operator==(const Point& other) const {
    return x == other.x && y == other.y;
  }
  bool operator<(const Point& other) const {
    return x < other.x || (x == other.x && y < other.y);
  }
};
```

### 2. 常见注意事项

- **保持语义一致性**  
  重载运算符时，建议保持与内置类型类似的语义。例如，`operator<` 应该只表示“小于”，不要混入其他逻辑。

- **const 修饰**  
  比较运算符通常声明为 `const` 成员函数，保证不会修改对象状态。例如：

  ```cpp
  bool operator==(const Point& other) const;
  ```

- **与 STL 容器配合**  